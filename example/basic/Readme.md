# Basic

Basic example with a view and controller.

## Running locally

1. Run `npm install` to install the required `node_modules`.
2. From this directory, run `npm link cox722 ../../cox722` to link `node_modules/cox722` to the runtime.
